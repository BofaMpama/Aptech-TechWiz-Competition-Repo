import 'package:flutter/material.dart';

class AppColors {

  // Primary Colors
  static const Color primaryGreen = Color(0xFF2CB629);
  static const Color primaryYellow = Color(0xFFD3F303);
  static const Color primaryBlue = Color(0xFF2F80ED);
  static const Color lightGreen = Color(0xFFFAFFDD);
  static const Color white = Color(0xFFFFFFFF);

  // Error Color
  static const Color error = Color(0xFFFF0000);

  // Additional commonly used colors (optional)
  static const Color black = Color(0xFF000000);
  static const Color transparent = Colors.transparent;

  // Text colors based on your existing setup
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textLight = Color(0xFFFFFFFF);

  // Background colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color backgroundLight = Color(0xFFFAFFDD);

  // Border colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderActive = Color(0xFF2F80ED);
}